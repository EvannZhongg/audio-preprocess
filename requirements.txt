# NOCA:horus-dependency-security(ray/aiohttp/torch 依赖版本与训练/推理工具链兼容性强绑定,升级需评估,暂忽略安全告警)
# Core AI/ML Framework
# NOTE: torch/torchaudio/torchvision are installed via conda in Dockerfile,
# do not install them again via pip to avoid version conflicts.
# torch==2.5.0
# torchaudio==2.5.0
# torchvision==0.20.0


# Diarization & Speaker Verification
# Using a specific version of pyannote.audio for stability.
# This will pull pyannote.core, pyannote.pipeline, etc. as dependencies.
pyannote.audio==3.3.2
pyannote.core==5.0.0
pyannote.database==5.1.3
pyannote.metrics==3.2.1
pyannote.pipeline==3.0.1
modelscope[audio]
# Explicitly add dependencies that caused issues before
addict
datasets
einops
scikit-learn
scipy

# ASR Models
# openai-whisper
google-generativeai
# funasr: must be >=1.3 for Fun-ASR-MLT-Nano-2512 support.
# (We previously pinned 1.2.4 then upgraded in dockerfile; consolidated here.)
funasr>=1.3.0
aiohttp==3.9.2
faster-whisper==1.1.0
whisperx>=3.3.0
jiwer==4.0.0

# LLM-based scoring (PPL via Qwen3-0.6B + LLM API for text/domain quality)
# transformers upper-bound: 4.55+ requires torch>=2.7 for FP8 dtypes
# (torch.float8_e8m0fnu); we ship torch=2.5.0 in dockerfile, so cap at <4.55.
# 4.51 is the lowest version with Qwen3 architecture support.
transformers>=4.51,<4.55
huggingface_hub>=0.24.0
requests>=2.31.0

# Audio-quality metrics
./third_party/brouhaha-vad-main.zip

# Audio Processing & File Handling
librosa
pydub
soundfile
pandas
openpyxl

# MFA & ASR Validation
# praatio

# Utilities
tqdm
pyyaml
filelock

silero-vad
mutagen
ray==2.48.0
concurrent-log-handler==0.9.29

# Text quality scoring
language-tool-python>=2.7

# ASR APIs (Qwen3-ASR via Polaris service discovery)
--extra-index-url https://mirrors.cloud.tencent.com/pypi/simple/
polaris-cpp-py
